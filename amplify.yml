version: 1
backend:
  phases:
    build:
      commands:
        - cd amplify && npm install && cd ..
        - ./amplify/node_modules/.bin/ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID || echo "Backend deployment skipped"
frontend:
  phases:
    preBuild:
      commands:
        - npm install --prefix web-app
    build:
      commands:
        - npm run build --prefix web-app
  artifacts:
    baseDirectory: web-app/out
    files:
      - '**/*'
  cache:
    paths:
      - web-app/node_modules/**/*
      - web-app/.next/cache/**/*
      - amplify/node_modules/**/*
